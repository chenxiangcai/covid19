/*
import {put,select} from 'redux-saga/effects'
import * as types from './action-types'
import {get} from '../../utils/http'

function *loadChinaMapData(){
    console.log(3)
    try {
       const  data = yield select(state => (console.log(state)))
        console.log(data)
    }catch (e) {
        console.log(e)
    }
}

export {types, loadChinaMapData as action}*/
